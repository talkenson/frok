/**
 * This macro represents build timestamp in HH:mm:ss dd.MM.yyyy format
 */
declare const $buildTime: string

/**
 * This macro represents build mode (version) like `prod` or 'stage'
 */
declare const $buildMode: string
